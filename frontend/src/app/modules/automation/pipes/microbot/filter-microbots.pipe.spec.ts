import { FilterMicrobotsPipe } from './filter-microbot.pipe';

describe('FilterMicrobotsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMicrobotsPipe();
    expect(pipe).toBeTruthy();
  });
});
