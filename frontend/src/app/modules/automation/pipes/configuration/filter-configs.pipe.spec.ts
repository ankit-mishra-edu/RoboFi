import { FilterConfigurationsPipe } from './filter-configs.pipe';

describe('FilterConfigurationsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterConfigurationsPipe();
    expect(pipe).toBeTruthy();
  });
});
