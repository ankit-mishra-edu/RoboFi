import { FilterSpecificationsPipe } from './filter-specifications.pipe';

describe('FilterSpecificationsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSpecificationsPipe();
    expect(pipe).toBeTruthy();
  });
});
